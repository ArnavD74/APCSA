package arraylistMethods;

public class Main {
	
	public static void main (String[] args) {
		
		/*add: adds x to end of list by default or sandwiches into element
		*clear(): removes all elements
		*clone(): creates shallow copy
		*contains(x): returns true if arraylist includes x
		*indexOf(x): returns the index of the first element x or -1 if DNE
		*isEmpty(): true if 0 elements
		*lastIndexOf(x): like indexOf but returns last index
		*remove(x): removes object at position x
		*removeRange(int start, int end)
		*set(int x, e element)
		*size(): returns length
		*/
		
		/*
		 * copyOf: copies int[]
		 * copyOfRange: copies array from range (array, int start, int end)
		 * sort(int[] a) sorts array in ascending numerical order
		 * toString(int[] a) prints as string
 		 */
		
	}

}
